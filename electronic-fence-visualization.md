# 电子围栏可视化调试功能

## 概述
为了方便调试AI agent的电子围栏功能，我在地图上添加了可视化显示，让你能够清楚地看到agent的活动限制区域。

## 可视化特性

### 1. 红色围栏边界
- **红色半透明填充**: 显示agent可活动的完整区域
- **红色边框**: 清晰标记围栏边界
- **角标标记**: 四个角落有特殊的L型标记
- **网格线**: 显示每个瓦片的边界（当瓦片≥32像素时）

### 2. 警告标签
- **顶部警告条**: 显示"🚨 AGENT ELECTRONIC FENCE"和具体坐标范围
- **脉冲动画**: 警告条有闪烁效果，提醒这是调试功能
- **坐标信息**: 显示精确的边界 (x: 35-44, y: 0-14)

### 3. 禁止符号
- **中心圆圈**: 在围栏中心显示红色禁止符号
- **斜线标记**: 表示该区域对agent活动有限制

## 启用调试模式

### 方法1: 环境变量 (推荐)
在 `.env.local` 文件中添加：
```bash
VITE_SHOW_DEBUG_UI=true
```

### 方法2: 临时启用
运行时设置环境变量：
```bash
VITE_SHOW_DEBUG_UI=true npm run dev
```

## 可视化效果描述

当你启用调试模式后，你会看到：

1. **地图右上角**的红色半透明区域 - 这就是帐篷区域
2. **红色边框**标记了agent无法越过的界限
3. **顶部红色警告条**显示围栏的具体坐标范围
4. **网格线**帮助你理解瓦片坐标系统

## 调试用途

### 验证电子围栏功能：
- ✅ 确认agent只在围栏内生成
- ✅ 验证agent无法走出围栏边界
- ✅ 观察agent的路径寻找如何适应围栏限制
- ✅ 确认human玩家不受围栏影响

### 地图坐标理解：
- **地图大小**: 45×32瓦片
- **围栏位置**: x: 35-44, y: 0-14 (右上角)
- **瓦片大小**: 32×32像素
- **原点位置**: 左上角 (0,0)

## 文件结构

### 新增文件：
- `src/components/AgentFence.tsx` - 电子围栏可视化组件

### 修改文件：
- `src/components/PixiGame.tsx` - 集成AgentFence组件
- `.env.local` - 启用调试UI

## 测试步骤

1. **启动开发服务器**：
   ```bash
   npm run dev
   ```

2. **观察电子围栏**：
   - 你应该在地图右上角看到红色围栏
   - 顶部应该有警告条显示坐标信息

3. **测试agent行为**：
   - 创建新的agent，它们应该在围栏内生成
   - 观察agent移动，它们不会越过红色边界
   - Human玩家应该可以自由穿过围栏

4. **路径寻找测试**：
   - 让agent尝试移动到围栏外的位置
   - 观察路径寻找算法如何处理边界限制

## 注意事项

- **调试模式**: 警告标签会出现在页面顶部，可能遮挡部分UI
- **性能**: 可视化组件使用轻量级PixiJS Graphics，对性能影响很小
- **清理**: 警告标签组件会在组件卸载时自动清理DOM元素

## 禁用可视化

当你完成调试后，可以通过以下方式禁用可视化：

1. **删除环境变量**：
   ```bash
   # 从 .env.local 中删除这行
   # VITE_SHOW_DEBUG_UI=true
   ```

2. **重启开发服务器**：
   ```bash
   npm run dev
   ```

电子围栏功能会继续工作，只是不再显示可视化边界。

## 故障排除

如果看不到可视化围栏：

1. **检查环境变量**：
   ```bash
   echo $VITE_SHOW_DEBUG_UI
   ```

2. **检查控制台错误**：
   - 打开浏览器开发者工具
   - 查看是否有JavaScript错误

3. **确认调试UI启用**：
   - 检查 `SHOW_DEBUG_UI` 变量的值
   - 确保其他调试元素（如DebugTimeManager）也正常显示

现在你可以清楚地看到电子围栏，方便调试和验证agent的活动范围了！🎯